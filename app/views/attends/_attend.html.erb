<div id="going">

	<p>
	  <strong><%= attend.attendee %></strong>
	  
	</p>
  <%- if user_signed_in? %>

	<%- if @user.username == attend.attendee %>

	  <%= link_to 'not going', [attend.article, attend],
	               method: :delete,
	               class: "btn btn-default" %>
	<%- end %>
		<%- end %>

</div>

